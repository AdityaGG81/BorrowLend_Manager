<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!-- Box fill color -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Box border color -->
    <corners android:radius="40dp"/> <!-- Rounded corners if desired -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>